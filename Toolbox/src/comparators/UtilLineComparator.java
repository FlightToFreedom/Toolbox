package comparators;


public class UtilLineComparator {
	// public Comparator<Line> utcComparator=new Comparator<Line>(){
	//
	// @Override
	// public int compare(Line one, Line two)
	// {
	// Long utcOne = Long.parseLong((String) one.get(1));
	// Long utcTwo = Long.parseLong((String) two.get(1));
	// return utcOne.compareTo(utcTwo);
	// }
	//
	// };
}
